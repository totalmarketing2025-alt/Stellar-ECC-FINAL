name: Build Flutter APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.24.0"
          channel: stable

      - name: Create local.properties
        run: |
          echo "sdk.dir=$ANDROID_HOME" > android/local.properties

      - name: Get dependencies
        run: flutter pub get

      - name: Fix flutter_jailbreak_detection namespace
        run: |
          FILE=$(find ~/.pub-cache -path "*flutter_jailbreak_detection*/android/build.gradle" | head -1)
          if [ -n "$FILE" ]; then
            sed -i '/android {/a\    namespace "com.flutter.jailbreak_detection"' "$FILE"
          fi

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: stellar-ecc-apk
          path: build/app/outputs/flutter-apk/app-release.apk
